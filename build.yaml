targets:
  $default:
    sources:
      - $package$
      - pubspec.*
      - lib/**
      - test/**
    builders:
      json_serializable:
        options:
          explicit_to_json: true
      graphql_codegen:
        options:
          clients:
            - graphql
          scalars:
            DateTime:
              type: DateTime
